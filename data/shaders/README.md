# Blade render shaders

Copied from the `blade-render` crate (`code/` directory), version matching the
`blade-engine` dependency (currently 0.6.x / crates.io).

License: MIT — see https://github.com/kvark/blade
